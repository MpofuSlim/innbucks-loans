meta {
  name: Bearer Token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/token
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "thomas_test_2",
    "password": "&#$5TCL47"
  }
}

script:post-response {
  bru.setEnvVar("token",res.body.access_token);
}
